export interface ITheme {
  white: {
    light: string
  }
  black: {
    main: string
  }
  primary: {
    blue: string
  }
  secondary: {
    green: string
  }
  tertiary: {
    grey: string
  }
  error: string
}
