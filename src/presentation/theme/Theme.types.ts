export interface ITheme {
  white: {
    light: string
  }
  black: {
    main: string
  }
  primary: {
    blue: string
  }
  secondary: {
    green: string
  }
  tertiary: {
    linear: string
    background: string
    grey: string
  }
  error: string
}
